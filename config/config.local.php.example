<?php
/**
 * Local / hosting overrides for ProProfessor AI.
 * Edit DB credentials for zayith.com hosting, then re-upload this file.
 */
declare(strict_types=1);

return [
    'env' => 'production',
    'debug' => true, // set false after everything works

    // Uncomment only if auto base path detection fails:
    // 'base_url' => '/demo/professor',

    'db' => [
        'host' => 'localhost',
        'port' => 3306,
        'name' => 'YOUR_DB_NAME',
        'user' => 'YOUR_DB_USER',
        'pass' => 'YOUR_DB_PASSWORD',
        'charset' => 'utf8mb4',
    ],
];
