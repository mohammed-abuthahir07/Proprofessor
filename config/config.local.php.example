<?php
/**
 * Local / hosting overrides for ProProfessor AI.
 * Edit DB credentials for zayith.com hosting, then re-upload this file.
 */
declare(strict_types=1);

return [
    'env' => 'production',
    'debug' => true, // set false after everything works

    // Uncomment only if auto base path detection fails:
    // 'base_url' => '/demo/professor',

    // Optional: encrypt professor AI API keys. Prefer env PPAI_ENCRYPTION_KEY in production.
    // 'encryption_key' => 'set-a-long-random-string',

    'db' => [
        'host' => 'localhost',
        'port' => 3307,
        'name' => 'YOUR_DB_NAME',
        'user' => 'YOUR_DB_USER',
        'pass' => 'YOUR_DB_PASSWORD',
        'charset' => 'utf8mb4',
    ],
    // Optional notification providers (disabled until real credentials are set via env):
    // 'notifications' => [
    //     'email' => ['enabled' => true, 'from' => 'noreply@yourcollege.edu'],
    //     'whatsapp' => ['enabled' => false, 'provider' => '', 'api_key' => ''],
    //     'sms' => ['enabled' => false, 'provider' => '', 'api_key' => ''],
    // ],
];
